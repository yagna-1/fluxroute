apiVersion: v1
kind: Service
metadata:
  name: fluxroute-controlplane
  namespace: fluxroute
spec:
  selector:
    app: fluxroute-controlplane
  ports:
    - name: http
      port: 8081
      targetPort: http
