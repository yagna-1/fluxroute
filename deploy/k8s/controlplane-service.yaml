apiVersion: v1
kind: Service
metadata:
  name: agent-router-controlplane
  namespace: agent-router
spec:
  selector:
    app: agent-router-controlplane
  ports:
    - name: http
      port: 8081
      targetPort: http
