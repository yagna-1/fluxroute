apiVersion: 1

providers:
  - name: agent-router
    orgId: 1
    folder: Agent Router
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
