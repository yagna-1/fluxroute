# OpenAPI Contracts

- `router-v1.yaml`: Router runtime endpoints (`/v1/*`)
- `controlplane-v1.yaml`: Control-plane endpoints (`/v1/*`)

Legacy non-versioned routes remain available as aliases in the server handlers.
